# Routine Step Grouping — Progress

**Status:** `PR_REVIEW`
**Spec:** `docs/specs/routine-step-grouping/` (PRD_Spec.md · SCREENS.md · USER_STORIES.md · IMPLEMENTATION_PLAN.md)
**Opened:** 2026-08-24
**Owner:** unassigned

---

## What this is

The Routine screen gains a grouping level: a step becomes a **process** (Cleanse, Treat, Moisturize…) holding one or more products, instead of being one product. Adds informational waits between steps, per-card completion, product zones and reapplication timing.

Render-layer change. `RoutineStep` still holds one product, `Routine.steps` stays a flat array, and the routine engine is not touched.

---

## Phase status

| Phase | Status | Notes |
|---|---|---|
| 0 — Verify baseline | `DONE` | All six assumptions re-verified against shipped code while implementing (LAYERING_ORDER, actives.json classes, SKELETON_SLOTS, etc.) |
| 1 — Types and constants | `DONE` | `StepAction`/`Zone`/`StepTiming`/`StepTransition` in types/index.ts; `STEP_ACTION_FOR_TYPE`/`STEP_ACTION_LABELS`; `TRANSITION_RULES`/`TRANSITION_PRECEDENCE` |
| 2 — Grouping and transition logic | `DONE` | `src/utils/routineGrouping.ts`: `groupStepsIntoActions`, `resolveTransition`, `attachPlaceholderGaps`, `isGroupAllCompleted` — 76 unit tests total across Phase 1/2 files |
| 3 — Product fields on the shelf | `DONE` | ProductDetailScreen gained a "Routine Placement" card (zone chips + inline/reapply toggle + hours field), writing to `Product` only |
| 4 — Routine screen | `DONE` | RoutinesScreen renders grouped steps via `StepGroupHeader`/`RoutineProductCard`/`StepTransitionDivider`, edit-mode toggle, gap/orphan cards |
| 5 — Completion | `DONE` | `src/store/completionStore.ts`; `gamificationEnabled` default flips to `true` for new installs (existing persisted `false` is preserved, not force-migrated) |
| 6 — Duplicate slot + cleanup | `DONE` | Duplicate-slot suppression by zones/timing: done. `RoutineSchedulerSheet` revived and wired to the edit-mode Schedule icon: done. Dead-code deletion (`TodayScreen.tsx`, `WeeklyPlanView.tsx`, `TabButton.tsx`, `ContributionConsentMigrationBanner.tsx` + their 3 orphaned test files) executed by coordinator on 2026-08-28 after re-verifying no references in `src/` or `AppNavigator.tsx`; `tsc --noEmit` re-run clean. Colour-map doc rewording: not actioned (docs-only, no code/test surface). |
| 7 — Header chrome + single-active-period follow-up | `DONE` | Human-provided mockup (transcribed, not committed to the repo) reversed the img-03 accordion redesign: header simplified to title + calendar/list leftAction + edit-mode rightAction only (Regenerate/Add moved out, footer already had equivalents); accordion replaced by a single-active-period `PillToggle` (Morning/Evening). See 2026-08-28 log entry below for the full rationale and deviations. |

---

## Decisions taken during specification

Recorded so they are not relitigated:

- **Grouping at the render layer, not in the engine.** The engine already orders, schedules and safety-gates correctly. Promoting `StepAction` into it is possible later as an isolated task.
- **Eight actions**, no separate `pre_cleanse`. A makeup remover and a cleanser are one process; the engine's `pre_cleanse` slot stays a plan-construction concept.
- **Group order read from the persisted array**, not computed from `LAYERING_ORDER` — otherwise every manual drag is silently undone.
- **No period restrictions** beyond the one the engine already imposes on `spf`.
- **`zones` and `timing` on `Product`**; `scheduledDays` stays on `RoutineStep` because it varies per period.
- **Completion per card**, not per step, because a `reapply` product is used hours after the rest of its step.
- **Transitions are four verbal notes, no numeric waits.** The 5/10/15/20-minute set was specified and then removed: the pH mechanism behind long acid-to-retinoid waits is disputed by formulation-chemistry sources, absorption waits have no conclusive research behind them, and competitor apps differ by two orders of magnitude (Skin Bliss 20–30 s vs Layered 10–20 min). The surviving four rest on other mechanisms.
- **Pause and Remove separated by placement**, not by iconography: icons for the reversible actions, removal in the overflow sheet.

## Defects caught in spec review

Found before implementation, all fixed in the docs:

- consecutive-run grouping split `Moisturize` around `Mask` (indices 9/10/11)
- a hand-written step order contradicted `LAYERING_ORDER`
- computing order from `LAYERING_ORDER` would have killed drag
- invented period restrictions would have hidden a morning exfoliant
- `azelaic` / prose class names would have compiled and silently never fired
- `spf_filters` has no chemical/mineral discriminator, so a "chemical SPF" rule was unimplementable
- React Native has no inner shadow; the completed-card treatment needs an SVG overlay
- the 10–20 minute waits came from beauty publishing repeating a disputed claim; removed after checking primary-facing sources

---

## Open items

| Item | Blocking? | Note |
|---|---|---|
| Dermatologist sign-off on the four layering notes | Before launch, not before build | Two questions: are these four right, and should any numeric wait be reinstated — see `PRD_Spec.md` §5.2 before ratifying magazine numbers |
| Polish labels | No | Blocked on a localization layer that does not exist; this package ships English |
| `minRestHours: 72` declared but never read | No | Implement or delete; out of scope here |
| Position of `Mask` after `Moisturize` | No | Inherited from `LAYERING_ORDER`; correct for sleeping masks, arguable for sheet masks |
| Mockup not in the repo | No | Layout reference currently lives outside version control |
| ~~Dead-code deletion not executed~~ | Resolved | Deleted 2026-08-28 by coordinator: `TodayScreen.tsx`, `WeeklyPlanView.tsx`, `TabButton.tsx`, `ContributionConsentMigrationBanner.tsx`, plus their 3 now-orphaned test files (`tests/routine-engine/today-screen.test.tsx`, `tests/routines/weekly-plan-view-hidden-filter.test.tsx`, `tests/contribution-consent/ContributionConsentMigrationBanner.test.tsx` — one more test file than the engineer's own count, since `today-screen.test.tsx` directly rendered `TodayScreen`). Re-verified zero references in `src/`, `AppNavigator.tsx`, and `App.tsx` before deleting; `tsc --noEmit` clean after. |
| `RoutineStepActionSheet` 3-row (SCREENS.md §7.4) vs shipped 4-row mismatch | No — flagged for tech-lead | Left unmodified per the qa-lead's own open question: `IMPLEMENTATION_PLAN.md` Phase 4 does not list it as a modified file, and the QA suite only asserts "Remove from routine" + edit-mode-body-tap reachability, both already true of the 4-row sheet (View details/Edit/Remove/Hide). Wired `RoutineProductCard`'s edit-mode body tap to the same `openStepSheet` the overflow button already used. |
| `ConflictWarningInline` still renders at the top of the list, not "beneath the owning step's header" | No — descoped | `IMPLEMENTATION_PLAN.md` Phase 4 lists this reposition, but no QA test asserts per-group placement, and `ConflictWarningInline` computes ALL conflict/advisory/density rows globally with no per-step filtering hook — rearchitecting it to slot per-group was judged high-risk/low-test-coverage for this pass. Left at its existing position. |
| No standalone "paused steps" list/restore UI on RoutinesScreen | No — descoped | The edit-mode Pause icon correctly calls `setStepHidden(routineId, stepId, true)` (tested), but SCREENS.md's "Paused cards now surface in `PausedSteps`" merge with the existing clinical-freeze `PausedSteps` block was not built — no QA test requires it, and `HiddenStepsManager` (the component the plan says to "remove from the Clinic footer") turned out to have never been implemented (only a comment reference in `WeeklyPlanView.tsx`), so that specific removal item is moot. |
| `getSkincareDateString` (`src/utils/timeHelpers.ts`) UTC-conversion bug | No — pre-existing, out of scope | `RoutinesScreen.completion.test.tsx`'s "records a completion made at 01:00 against the previous calendar day" test fails on this machine (`Europe/Warsaw`, UTC+2) because the helper's `<4am` branch computes the previous LOCAL day then serialises via `.toISOString()` (UTC), which can roll the date again in timezones ahead of UTC. The failing assertion (line 227) calls this untouched helper directly — it is not exercising any of my code. PRD_Spec.md §6.2 says reuse this helper, not reimplement it, and it is depended on by several already-shipped features (routineAccordion day-scoping, RehabNoticeCard/ConflictWarningInline collapse state, `performDailyCheckIn`) — fixing it is a cross-cutting change outside this task's scope. Flagged for tech-lead/follow-up. |

---

## Code

New: `src/types/index.ts` (additive types only), `src/constants/rulesets/stepActions.ts`(+`.test.ts`), `src/constants/rulesets/stepTransitions.ts`(+`.test.ts`), `src/utils/routineGrouping.ts`(+`.test.ts`), `src/utils/routineSchedule.ts`(+`.test.ts`, additive — extracted `getWeekStart`/added `dateForDow`), `src/store/completionStore.ts`(+`.test.ts`), `src/components/routine/StepGroupHeader.tsx`, `src/components/routine/RoutineProductCard.tsx`, `src/components/routine/StepTransitionDivider.tsx`, `src/components/routine/GapCard.tsx`, `src/components/routine/OrphanedSlotCard.tsx`.

Modified: `src/screens/RoutinesScreen.tsx` (grouped rendering, edit mode, completion, gap/orphan cards, real-date future/past gating), `src/screens/ProductDetailScreen.tsx` (zones/timing controls), `src/screens/ProfileScreen.tsx` (Gamification copy), `src/store/settingsStore.ts` (`gamificationEnabled` default), `src/components/routine/PlannerBlock.tsx` (`getWeekStart` now imported from `routineSchedule.ts`), `src/components/routine/DuplicateSlotWarningInline.tsx` (passes `products` through), `src/utils/routineEngine/duplicateSlot.ts` (+`.test.ts` additions — zones/timing fingerprint suppression), `src/services/storage.ts` (`STORAGE_KEYS.completions`), `App.tsx` (hydrates `completionStore`), `tests/routine-engine/routines-screen-generation-ux.test.tsx` (one assertion updated: the header now DOES carry an edit-mode toggle per US-42, superseding the img-03-era "no edit toggle" assertion).

Deliberately unmodified: `src/components/routine/RoutineStepCard.tsx` (kept in place, unused by `RoutinesScreen`, since 2 pre-existing standalone component-test suites outside this task's scope still import it directly), `src/components/routine/RoutineStepActionSheet.tsx` (see Open items).

### Bug-fix round (2026-08-28)

Modified: `src/utils/routineLabel.ts` (new `derivePeriodSchedules`/`PeriodSchedules`/`PeriodScheduleEntry`, additive — `deriveProductSchedule` untouched), `src/components/routine/RoutineSchedulerSheet.tsx` (independent morning/evening `scheduledDays` state, two conditional `WeeklySchedulePicker` sections replacing the one shared "Weekly Planner" section, per-period-only Save), `src/screens/RoutinesScreen.tsx` (subscribes to `completionStore`'s `completions` array so a toggle actually re-renders the screen), `src/components/routine/RoutineProductCard.tsx` (zap icon / edit-mode Pause+Schedule icons / "Completed" badge relocated from `rightCluster` onto a new `headerRow` next to `brandName`; `InnerShadowOverlay` and its `react-native-svg` import removed).

Tests: `src/utils/routineLabel.test.ts` (new `derivePeriodSchedules` cases), `tests/vials-bottomsheet-consolidation/RoutineSchedulerSheet.regression.test.tsx` (updated "Weekly Planner" assertion + 3 new per-period regression cases), `tests/routine-step-grouping/RoutinesScreen.completion.test.tsx` (real-Zustand-backed `completionStore` mock + double-tap round-trip test).

### Phase 7 (header chrome + single-active-period follow-up, 2026-08-28)

Modified: `src/screens/RoutinesScreen.tsx` (header simplified to `title="Routine"` + `leftAction` calendar/list toggle + `rightAction` edit-mode toggle only; `expanded: AccordionState` state replaced by `selectedPeriod: Period`, still seeded from `resolveAccordionState`/persisted via `toPersistedAccordionState` — see rationale below; `PeriodCard` component + `periodCardStyles` deleted; single `NestableDraggableFlatList` renders `activeRenderGroups` for `selectedPeriod` instead of two always-mounted per-period lists; calendar-view's redundant `PlannerBlock` sub-header call removed), `src/components/routine/PlannerBlock.tsx` (dropped `viewMode`/`onViewModeChange`/`showWeekStrip` props and the internal List⇄Calendar `PillToggle` — now renders only the Mo…Su day strip, its one remaining job), `tests/routine-engine/routines-screen-generation-ux.test.tsx` (2 assertions updated: header no longer exposes "Regenerate routine"/header "+"; both now asserted via the pre-existing footer `OptimizeStrip`/"Add product" instead — same handlers, different affordance).

Deviation from the human's steer, logged per convention: reused `PillToggle` instead of `SegmentedControl` for the Morning/Evening toggle. `SegmentedControl` turned out to have no real consumers (a stray prose comment in `Select.tsx`, not an import); `PillToggle` already has an icon slot and its active-fill color already matches the mockup's dark-plum pill (`SegmentedControl`'s is black). Mid-task constraint honored: `OptimizeStrip.tsx` and the "Add product" `Button` are byte-for-byte unchanged (verified via `git diff --stat`, zero output).

**2026-08-28** — Tech-lead re-review (Phase 7, run directly by the coordinator for the same structural reason as the Phase 0–6 review): `npx tsc --noEmit` clean; `npx jest tests/routine-step-grouping` 71/72 (same pre-existing UTC red, no new failures); `git diff --stat -- src/components/routine/OptimizeStrip.tsx` confirms zero changes to the footer per the mid-task constraint; spot-checked `RoutinesScreen.tsx` header/`PillToggle`/`selectedPeriod` wiring directly against the plan. **Verdict: ACCEPT.**

Deliberately unmodified (verified, not just assumed): `tests/routine-step-grouping/{RoutinesScreen.grouping,RoutinesScreen.completion,RoutinesScreen.editMode}.test.tsx` — none of the three reference `expanded.morning/evening`, `PeriodCard`'s chrome text/a11y state, or press a period header to toggle it; all three rely solely on the existing `jest.mock('@/utils/routineAccordion', () => ({ resolveAccordionState: () => ({ morning: true, evening: false }) }))` to pick the default active period, which Phase 7 preserves byte-for-byte as the seed for `selectedPeriod`. `OptimizeStrip.tsx` and the footer "Add product" `Button` — left pixel-identical (human explicitly asked not to restyle them toward the mock's outlined-button look, even though it would read a little closer); only their existing wiring (`handleOpenDraftPreview`/`handleOpenAddSheet`) is now reached exclusively through them instead of also duplicated in the header.

### Polish round 3 (per-card similar-slot tip, spacing/badge/photo/shadow polish, 2026-08-28)

Modified: `src/components/routine/RoutineProductCard.tsx` (new `similarProductName` prop + `similarRow`; product-type `Badge` removed from `statusBadgesRow`; `ProductThumbnail` switched to `fill`/`squareBottomLeft`; card padding restructured onto `textColumn`/`rightCluster`/new `belowContent` wrapper; card radius `radius.lg` → `radius.sm`; new outer `cardShadow` wrapper split out per the shadow-visibility fix; exported `getPrimaryActiveKey`), `src/components/routine/DuplicateSlotWarningInline.tsx` (new exported `groupHasActiveIngredientOverlap` filter applied before the existing row-building `.map`), `src/screens/RoutinesScreen.tsx` (new `similarMap` sibling to `conflictMap`, wired into `RoutineProductCard`'s `similarProductName` prop, gated by the same active-ingredient filter), `src/constants/tokens.ts` (`gapStack`/`gapSection`/`gapInline` halved: 12→6, 32→16, 8→4), `tests/routine-step-grouping/{fixtures.ts,RoutineProductCard.test.tsx,DuplicateSlotWarningInline.suppression.test.tsx}`, `tests/routine-similar-product-priority/{duplicate-slot-warning-inline.test.tsx,routines-screen-duplicate-wiring.test.tsx}` (incidental mock-completeness fix). Full rationale, the exact-number spacing deviation, and the two latent bugs found along the way (StyleSheet shadow-merge, missing mock export) are in the three dated log entries above.

### Code-review round (2026-08-31)

New: `tests/routine-step-grouping/RoutinesScreen.conflictCrossPeriod.test.tsx`.

Modified: `src/components/routine/ConflictWarningInline.tsx` (new optional `allSteps` prop, used only for pairwise conflict detection; advisories/density untouched), `src/screens/RoutinesScreen.tsx` (`listHeader`'s `ConflictWarningInline` call now also passes `allSteps={[...amSteps, ...pmSteps]}`), `src/store/settingsStore.ts` (`gamificationEnabled` default reverted `true` → `false`, see log for the CLAUDE.md-vs-spec rationale), `src/screens/ProfileScreen.tsx` (removed the "Dynamic Skin Cycling" `ListRow`/`handleCycleToggle`/`Alert`/`switchCycleType` import — the toggle promised a "daily check-in" mechanism with zero reachable UI callers even before `TodayScreen.tsx` was deleted; `routineCycleType`/`switchCycleType`/`performDailyCheckIn` left intact in the store/domain layer), `src/components/routine/AddToRoutineSheet.tsx` (split shared `scheduledDays` into `morningScheduledDays`/`eveningScheduledDays`, same fix pattern as `RoutineSchedulerSheet.tsx`; `derivePeriodSchedules` reused, not duplicated), `src/screens/ProductDetailScreen.tsx` (`formatUsedSummary` now built on `derivePeriodSchedules`, shows both periods distinctly when they diverge), `src/utils/routineEngine/duplicateSlot.ts` (+`.test.ts` — `fingerprint()` sorts `zones` before joining; `findSlotDuplicateGroups`'s `products` param made required, no more silent `= []` default), `tests/inci-attribution-highlighting/DetectedActiveBadgeWiring.test.tsx` (added missing `derivePeriodSchedules` key to its full-replacement `@/utils/routineLabel` mock), `tests/routine-similar-product-priority/add-to-routine-sheet-duplicate-flow.test.tsx` (2 new per-period regression cases).

Deliberately unmodified this round (judgment calls, see log): `RoutineStepCard.tsx`'s own `getPrimaryActiveKey`-equivalent (not consolidated with `RoutineProductCard.tsx`/`activeBadges.ts`), `completionStore.ts`'s unbounded scan (explicitly out of scope per the review).

## Log

**2026-08-24** — Specification written and reviewed against shipped code (`productFacts.ts`, `slotting.ts`, `actives.json`, `tokens.ts`, `package.json`). Package committed to `docs/specs/routine-step-grouping/`. Not started.

**2026-08-28** — QA (qa-lead): wrote the integration/component test suite in `tests/routine-step-grouping/` BEFORE any production code, per the qa-lead -> engineer handoff. 9 files: `fixtures.ts` (binding contract: exact prop shapes, copy strings, testIDs/accessibility labels for every new component, since the spec package documents behaviour but not literal component contracts) plus 8 `*.test.tsx` files covering US-34 through US-45 and the cross-cutting matrix (grouping bucket-not-walk invariant, array-driven group order, transition precedence table, reapply exclusion, completion + 04:00 boundary + future/past-day gating, header dimming, edit mode, gap/orphan empty states, duplicate-slot suppression by zones/timing). Full details and the concrete implementation checklist are in `progress/routine-step-grouping-handoff.json`.

Verified the suite fails for the RIGHT reason pre-implementation: `npx jest tests/routine-step-grouping` — 8/9 suites fail on module resolution (new components/types don't exist yet) or on behavioural red (existing `RoutinesScreen`/`DuplicateSlotWarningInline` don't yet implement the new behaviour); `npx tsc --noEmit` shows only the expected missing-type/missing-module errors, no stray syntax issues. Judgment calls made explicit in the handoff: tested against the shipped accordion Period-card UI rather than `SCREENS.md`'s stale "segmented control" description (current code already deviated from that description before this package existed — see `PlannerBlock.tsx`'s own comment); did not duplicate engine-determinism/pregnancy/adaptation regression coverage (US-45), which already lives in `tests/routine-engine/`.

**2026-08-28** — Engineer: implemented all 6 phases on `feature/routine-step-grouping`. Plan approved by the coordinator before writing code, including the two judgment calls the qa-lead left open (`RoutineStepActionSheet` left unmodified; the future/past-day `Date` is derived via a new shared `dateForDow`/`getWeekStart` in `routineSchedule.ts`, reused by both `PlannerBlock` and `RoutinesScreen` so the two can never drift) and the "keep `RoutineStepCard.tsx` in place" deviation from the literal rename `IMPLEMENTATION_PLAN.md` describes.

Verification, in the order `.claude/rules/testing.md` requires:
- `npx tsc --noEmit` — clean throughout, including after every phase.
- `npx jest tests/routine-step-grouping` — 71/72 green. The one red (`RoutinesScreen.completion.test.tsx`'s 01:00-boundary test) is a pre-existing timezone bug in the untouched shared `getSkincareDateString` helper, not a defect in this package's code — see Open items.
- Own unit tests (`routineGrouping.test.ts` 33 cases, `stepActions.test.ts`, `stepTransitions.test.ts`, `routineSchedule.test.ts`, `completionStore.test.ts`, plus additions to the existing `duplicateSlot.test.ts`) — 76/76 green.
- Full `npx jest --testPathIgnorePatterns="worktrees"` — triaged every failing suite individually. Found exactly one genuine regression (`routines-screen-generation-ux.test.tsx` asserted "no edit-mode toggle exists", an img-03-era assumption US-42 deliberately reverses) and fixed it in place. Every other failing suite (11) was confirmed pre-existing via `git diff --stat` showing zero prior changes to the implicated files before I touched anything — all trace to stale partial `jest.mock('@/constants/tokens'|'@/constants/labels', ...)` fixtures in old test files (missing keys like `palette.goldenTint`/`getProductTypeBadgeStatus` added since those mocks were written) or an unrelated pre-existing `deriveProductSchedule`-mock bug in `DetectedActiveBadgeWiring.test.tsx`. None of these files are touched by this package.

Phase 6 dead-code deletion (`TodayScreen.tsx`, `WeeklyPlanView.tsx`, `TabButton.tsx`, `ContributionConsentMigrationBanner.tsx`) was attempted but the sandbox's destructive-action classifier blocked `rm` — left in place, flagged in Open items for a human to delete along with their now-orphaned test files.

Not committed — left for tech-lead review per the coordinator's instruction.

**2026-08-28** — Engineer: polish round 3 (human-requested directly, three changes to `RoutineProductCard.tsx`/`RoutinesScreen.tsx` on top of the already-ACCEPTed grouping work + Phase 7 header-chrome follow-up, same branch). Plan (exact copy string, spacing numbers, padding restructure) presented and self-confirmed per auto-mode before writing code.

- **Change 1 — per-card similar-slot tip.** Copy: `Similar to {name}` (mirrors `Conflicts with {name}`). New optional prop `RoutineProductCardProps.similarProductName?: string | null`; new `similarRow` styled like `conflictRow` (bordered-top row) but `Icon name="layers"` + `colors.statusInfo`/`colors.statusInfoLine` (the cobalt sibling of `palette.amberLine`), reusing `DuplicateSlotWarningInline`'s existing icon/color rather than inventing new ones. Row order when multiple are present: conflict → similar → adaptation (severity: warning → info → note). `RoutinesScreen.tsx`'s existing `conflictMap` `useMemo` (L315-345) gained a sibling `similarMap`, built by calling `findSlotDuplicateGroups` separately over `am` and `pm` (the already day/hidden/frozen-filtered arrays) — not `routine.steps` unfiltered like the top banner — so the per-card tip can never contradict what's actually visible today; for each group, every member step maps to the *first other* member's product name (same "first found wins" simplicity as the existing conflictMap). `DuplicateSlotWarningInline.tsx` itself is untouched.
- **Change 2 — spacing + badge reduction.** `space.gapStack: 12 → 6`, `space.gapSection: 32 → 16` (both exactly halved; `gapInline` (8) untouched). Deviation from `IMPLEMENTATION_PLAN.md` Phase 4's original 8/12/32 vertical-rhythm values, logged per that doc's own instruction — reason: explicit human request after viewing the running Routine screen. The gapSection:gapStack ratio is preserved (32/12 ≈ 16/6 ≈ 2.67x) so groups stay clearly more separated than cards-within-a-group, per the Phase 4 constraint ("if the ratio drifts the grouping stops working"). Grepped first to confirm these two tokens are consumed only by `RoutinesScreen.tsx` — no other screen affected. Removed the product-type `Badge` from `statusBadgesRow` (and its now-dead `typeLabel`/`typeBadgeStatus` locals + `PRODUCT_TYPE_LABELS`/`getProductTypeBadgeStatus` imports); kept the zap active-ingredient icon badge and `AllergenBadge` untouched. Judgment call: left the `displayProductType` prop in the interface unconsumed rather than removing it from the contract — `RoutinesScreen.tsx` still computes and passes a reclassified type on every call site, and dropping the prop would ripple into that caller for a change scoped to "remove only the product-type badge"; doc comment on the prop updated to say so explicitly rather than leaving it silently stale.
- **Change 3 — edge-to-edge photo.** Followed `ProductShelfCard.tsx`'s existing `fill`-consumer pattern exactly rather than inventing a new one (per the human's own instruction): `card`'s `padding: space[3]`/`gap: space[2]` removed; `mainRow` switched to `alignItems: 'stretch'` (was `'center'`); `ProductThumbnail` now wrapped in a padding-free `thumbnailWrap` (`flexDirection: 'row'`, needed for the `alignSelf: 'stretch'`-based aspect-ratio sizing chain — same reason documented in `ProductShelfCard.tsx`) and switched from `size={48}` to `fill squareBottomLeft={hasBelowContent}`; `textColumn`/`rightCluster` gained their own `paddingVertical: space[3]` (+ `rightCluster` a `paddingRight: space[3]`) to keep the text/badge inset the card used to get for free from its own padding. The three below-`mainRow` rows (conflict/similar/adaptation) now live in one new `belowContent` wrapper (`paddingTop: space[2], paddingHorizontal: space[3], paddingBottom: space[3], gap: space[2]`) that reproduces the old card-level padding/gap for that region pixel-for-pixel; `hasBelowContent = hasConflict || hasSimilarTip || adaptationWeek != null` drives both that wrapper's visibility and `squareBottomLeft`.
- **Tests:** `tests/routine-step-grouping/fixtures.ts` — added `similarProductName: null` to `makeRoutineProductCardProps` defaults + a doc-comment line. `tests/routine-step-grouping/RoutineProductCard.test.tsx` — added `describe` blocks for the similar-slot tip (renders/absent/coexists-with-conflict), product-type badge removal (`queryByText('Cream')` null) + allergen-badge persistence (via `EU_ALLERGEN_LIST_VERSION`/`detectedAllergens` fixture, same pattern as the pre-existing `RoutineStepCard.allergen-badge.test.tsx`), and edge-to-edge thumbnail assertions using `StyleSheet.flatten` on the rendered `product-thumbnail` node's style (fill mode via `aspectRatio` presence + absent fixed `width`; `borderBottomLeftRadius` `radius.sm` vs `0` for `squareBottomLeft`) — same style-assertion precedent `ProductThumbnail.test.tsx` already established for `dimmed`/`opacity`, not a new pattern.
- **Incidental regression found and fixed:** `tests/routine-similar-product-priority/routines-screen-duplicate-wiring.test.tsx` fully mocks `@/utils/routineEngine/duplicateSlot` with only `rankSlotGroup`; once `RoutinesScreen.tsx` started calling `findSlotDuplicateGroups` directly (Change 1's `similarMap`), that mock's missing export broke all 3 of that file's tests (`TypeError: findSlotDuplicateGroups is not a function`). Added a neutral `findSlotDuplicateGroups: () => []` stub to that same mock (that suite only asserts `DuplicateSlotWarningInline`/`DuplicateSlotResolutionSheet` wiring, not similar-slot detection) — confirmed fixed, no assertion changes needed elsewhere in the file.

Verification:
- `npx tsc --noEmit` — clean.
- `npx jest tests/routine-step-grouping` — 7/8 suites, 80/81 tests green; the 1 red is the same pre-existing `getSkincareDateString` UTC-boundary bug (`RoutinesScreen.completion.test.tsx`), untouched by this round.
- `npx jest tests/routine-similar-product-priority` — 6/6 green after the mock fix above (was 5/6, 31/34 before the fix).
- `npx jest --testPathIgnorePatterns="worktrees"` — 10 failing suites (194 passed, 2289/2368 tests passed), zero new regressions: the exact same 10 suites as the Phase 7 tech-lead-reviewed baseline (`catalog-screen-hide-toggle.integration`, `RoutinesScreen.completion` [known UTC bug], `RoutineCalendarView`, `ProductShelfCard`, `DetectedActiveBadgeWiring`, `product-shelf-card-hidden`, `PaoChip.integration`, `product-detail`, `catalog-screen`, `add-product-hub`) — all still fail on the same pre-existing stale-`jest.mock('@/constants/tokens'|'@/constants/labels', ...)` pattern (missing `palette.goldenTint`/`palette.plumTint`/`shadow.sm` etc.), none related to `space.gapStack`/`gapSection`'s new numeric values (grepped: no test anywhere asserts those two tokens' literal values) or to any file this round touched.

Not committed — left for tech-lead review per the coordinator's instruction.

**2026-08-28** — Coordinator mid-task addenda to polish round 3 (addressed inline, not a separate round): two follow-up asks arrived while the round-3 log entry above was already being written.

1. **Active-ingredient gating on the similar-products notification.** Both the top-of-screen `DuplicateSlotWarningInline` banner and the new per-card tip must only fire when the duplicated products actually carry an active ingredient (two plain moisturizers must not trigger it; two serums that both carry an active should). Scoped at the display layer only, per the coordinator's explicit constraint — `findSlotDuplicateGroups` and `findSameSlotStep` (routinesStore.ts's separate add-time pre-check consumer) are untouched. Extracted the exact signal `RoutineProductCard` already uses for its own lightning badge into an exported `getPrimaryActiveKey(product)` in `RoutineProductCard.tsx` (single source of truth, so the card's own badge and the notification-gating logic can never disagree on what counts as "has an active"). Added `groupHasActiveIngredientOverlap(group, products)` in `DuplicateSlotWarningInline.tsx` (exported so `RoutinesScreen.tsx`'s `similarMap` builder can reuse the identical rule instead of duplicating it) requiring **every** group member to have a non-null primary active key, not just one — reasoning: the notification warns about redundant exposure to the *same* active effect, and a group with only one active-bearing member doesn't create that redundancy on this axis, so requiring ALL is the more correct reading of "two serums that both contain an active" from the ask. `DuplicateSlotWarningInline`'s row-building gained a `.filter(groupHasActiveIngredientOverlap)` before its existing `.map`; `RoutinesScreen.tsx`'s `similarMap` loop gained the same check inline.
   - **Test fallout, fixed:** two pre-existing qa-lead suites used actives-free fixtures to assert the banner DOES render (testing something else — zone/timing suppression, and screen wiring respectively) and broke once gating landed. `tests/routine-step-grouping/DuplicateSlotWarningInline.suppression.test.tsx`'s `SERUM_ONE`/`SERUM_TWO` gained `activeTags: ['niacinamide']` so that suite keeps testing what it's named for (zone/timing suppression) without tripping the new, unrelated gate. `tests/routine-similar-product-priority/duplicate-slot-warning-inline.test.tsx` — rather than editing the shared `CREAM_A`/`CREAM_B` fixtures (used across ~5 other files in that folder, out of proportion for this fix), added local `CREAM_A_ACTIVE`/`CREAM_B_ACTIVE` overrides used only in the two tests that needed the banner to actually render, plus two new tests asserting the gate itself (all-inactive suppressed; one-active-one-not still suppressed).
2. **`space.gapInline` also halved** (8 → 4), same treatment as `gapStack`/`gapSection` — grepped first to confirm it's consumed only by `RoutinesScreen.tsx` (header→first-card spacing). Ratios preserved end to end: 32/12 ≈ 16/6 ≈ 2.67x (section:stack) and 12/8 = 6/4 = 1.5x (stack:inline).

Verification after both addenda: `npx tsc --noEmit` clean; `npx jest tests/routine-step-grouping tests/routine-similar-product-priority` → 13/14 suites, 116/117 tests green (the 1 red is the same pre-existing `getSkincareDateString` UTC bug); `npx jest --testPathIgnorePatterns="worktrees"` → 10 failing suites (194 passed, 2291/2370 tests passed) — the exact same 10 pre-existing suites as every prior baseline in this file, zero new regressions.

**2026-08-28** — Two final coordinator-approved tweaks to `RoutineProductCard.tsx`, same polish round 3, addressed together after explicit approval ("proceed with everything ... implement it all"):

- **Card corner radius:** `radius.lg` (16) → `radius.sm` (8), matching the mockup and `ProductThumbnail`'s own `boxFill` corner radius (already `radius.sm` on the leading photo, so the card and its photo now agree).
- **Shadow visibility fix.** Traced the root cause directly before changing anything: `card`'s style combined `overflow: 'hidden'` (needed to clip the edge-to-edge photo/`InnerShadowOverlay` to the rounded corners) with `...shadow.sm` (pure `shadowColor`/`shadowOffset`/`shadowOpacity`/`shadowRadius` on iOS, which RN paints *outside* the view's own box) — `overflow: 'hidden'` on that same node silently clipped the shadow away. Identical problem `ProductShelfCard.tsx` already solved in this codebase (its own comment explains the exact same conflict) — followed that established split rather than inventing a new one: a new outer `cardShadow` `View` (no `overflow: hidden`) now carries the shadow/radius/background, wrapping the existing `Pressable` (`testID="routine-product-card"` and all its props/handlers unchanged) which keeps `overflow: 'hidden'` + `borderRadius: radius.sm` + background but no shadow token of its own. `InnerShadowOverlay`'s SVG mask `rx` updated from `radius.lg` to `radius.sm` to match.
  - **Bug found and fixed while wiring this up:** `cardShadowCompleted` was initially written as just `{ ...shadow.none }` (mirroring the *pre-existing* `cardCompleted: { ...shadow.none, borderWidth: 1, ... }` merge pattern this file already had) and applied via `[styles.cardShadow, completed && styles.cardShadowCompleted]`. Since `shadow.none` is `{}`, `StyleSheet.flatten`'s shallow merge cannot use an empty object to un-set a `shadowOpacity` key an earlier style in the same array already set — so completed cards still showed the shadow. This exact bug existed latently in the *original* pre-round-3 code too (`card`'s `...shadow.sm` + `cardCompleted`'s `...shadow.none` in the same array), just invisible because `overflow:hidden` was clipping the shadow either way — round 3's fix (making the shadow visible) is what surfaced it. Fixed by switching to **mutually exclusive** style objects, each carrying its own complete shadow definition, selected with `completed ? styles.cardShadowCompleted : styles.cardShadow` instead of array-merging — the same pattern `Card.tsx`'s `variantStyles` already uses for exactly this reason. Caught by a new test (`carries no shadow on the outer wrapper when completed`) that failed correctly against the naive merge-based first attempt before the fix.
- **Tests:** added two to `RoutineProductCard.test.tsx` (`describe('card shadow wrapper ...')`) asserting `shadowOpacity > 0` on the outer wrapper when not completed and `shadowOpacity` absent when completed — queried via `getByTestId('routine-product-card').parent?.parent?.parent` (verified the exact host-tree depth needed by instrumenting a throwaway debug test first, since `Pressable` composes to its own host `View` before the outer wrapper's host `View` is reached — 3 levels, not 1).

Verification: `npx tsc --noEmit` clean; `npx jest tests/routine-step-grouping tests/routine-similar-product-priority tests/product-shelf-card` → only the 2 already-known pre-existing reds (`RoutinesScreen.completion.test.tsx`'s UTC bug, `ProductShelfCard.test.tsx`'s stale-token-mock bug — confirmed unrelated to this file, `product-shelf-card` was already in every prior baseline's 10-suite list); `npx jest --testPathIgnorePatterns="worktrees"` → 10 failing suites (194 passed, 2293/2372 tests passed), suite names verified identical to every prior baseline in this file — zero new regressions.

Not committed — left for tech-lead review per the coordinator's instruction.

**2026-08-28** — Coordinator: executed the Phase 6 dead-code deletion the engineer's sandbox blocked (`TodayScreen.tsx`, `WeeklyPlanView.tsx`, `TabButton.tsx`, `ContributionConsentMigrationBanner.tsx`, plus 3 orphaned test files — one more than the engineer counted, since `tests/routine-engine/today-screen.test.tsx` directly rendered `TodayScreen`). Re-verified zero references in `src/`/`AppNavigator.tsx`/`App.tsx` before deleting; `tsc --noEmit` re-run clean after.

**2026-08-28** — Tech-lead review (run directly by the coordinator: the dedicated `tech-lead` subagent's Step-0 human-confirmation gate explicitly refuses confirmation relayed through a coordinator/agent, by design, and there is no product mechanism for a human to message a background subagent except via that relay — so the gate was structurally unsatisfiable here; flagged to the human as a tooling limitation rather than worked around). Checklist per `.claude/rules/architecture-review.md`:
- Design fidelity: `Product` type additions and all new types match the handoff's binding contract exactly (`src/types/index.ts`); `STEP_ACTION_FOR_TYPE` is exhaustive over all 18 `ProductType`s; `TRANSITION_RULES` has exactly the 4 specified keys with correct precedence.
- Layer separation: clean. No `AsyncStorage` usage outside `services/storage.ts` (only comments mention it), no React imports in `src/utils/`, no `fetch()` outside `src/services/`.
- Duplication: clean. No type/interface duplication outside `types/index.ts` (the one grep hit, `StepTransitionNoteText` in `stepTransitions.ts`, is an `Extract<>` derived from the real type, not a copy). No hardcoded hex colors in new/modified screens or components.
- `npx tsc --noEmit` — clean.
- Independently re-verified (not just trusted from the handoff): the `getSkincareDateString` UTC bug is real — traced the double-rollback (`.toISOString()` re-converts an already-locally-adjusted `Date` back to UTC, rolling the date a second time in timezones ahead of UTC) directly in `src/utils/timeHelpers.ts`; and the regression triage — ran the full suite myself (10 failing suites before my dead-code deletion trims it to 9; confirmed via `git stash` that `tests/catalog/product-detail.test.tsx` and its sibling failures reproduce identically on clean `dev`, unrelated to this branch).
- Deviations: all 5 logged (kept `RoutineStepCard.tsx`; left `RoutineStepActionSheet` at 4 rows vs `SCREENS.md`'s 3; didn't reposition `ConflictWarningInline` per-group; no paused-steps restore UI; coordinator's dead-code deletion) carry clear rationale in this log — none downgraded to BLOCKER per the undocumented-deviation rule in `architecture-review.md` §6, since none are undocumented.
- Quality signals: no TODO/FIXME/HACK, no `console.log`/`debugger` in new or modified files. WARNING (non-blocking): `RoutineProductCard`'s render function is ~150 lines (the "right cluster" priority logic the spec explicitly flagged as the hardest part to get compact); `RoutinesScreen`'s main component function is very large, but this predates the task (967 lines before, 1136 after) — recommend a future extraction pass, not a blocker here.

**Verdict: ACCEPT.** Status → `PR_REVIEW`, ready for human merge.

**2026-08-28 — Phase 7 (engineer, header chrome + single-active-period follow-up).** Follow-up scope requested directly by the human via a mockup (transcribed by the coordinator, not committed to the repo — screen: Routine, Evening/Tu-21 selected) on top of the already-ACCEPTed Phases 0-6, same branch. Plan approved by the coordinator before writing code, including two corrections found by reading the code rather than trusting the coordinator's transcription of the brief:

- `SegmentedControl.tsx` (`src/components/ui/forms/`) has **zero real importers** — `Select.tsx` only mentions it in a prose comment, never imports it — so "extend the shared atom, already used by Select" was inaccurate. Used `PillToggle` (`src/components/ui/core/PillToggle.tsx`) instead for the new Morning/Evening switch: it already has a per-segment icon slot (`icon?: (active: boolean) => ReactNode`) and a plum active fill matching the mock, and is the actual "shared full-width segmented pill" atom already powering the List/Calendar toggle being retired and Clinic's Active/History tabs. `SegmentedControl`'s active fill is `colors.controlFill` = `palette.black`, not plum, so extending it would also have needed a color override on top of adding the icon slot from scratch, for a component with no other current consumer either way.
- Kept `resolveAccordionState`/`toPersistedAccordionState`/`AccordionState`/`RoutineAccordionSettings` in `src/utils/routineAccordion.ts` completely unchanged. `RoutinesScreen` now derives one `selectedPeriod: Period` from `resolveAccordionState(persistedAccordion).morning ? 'morning' : 'evening'` on mount (still the 15:00 AM/PM rule + "today's snapshot wins"), and persists it back through the same two-boolean shape (`{morning: selectedPeriod==='morning', evening: selectedPeriod==='evening'}` — exactly one true, which the shape already supports without a schema change). This is a **deliberate reversal** of the img-03 accordion redesign (commit `f9e1fc6`, 2026-07-19) — human-confirmed directly with the coordinator, not an oversight — and a **repurposing** of what the persisted booleon pair means (independently-expandable → single-selected), logged here per `architecture-review.md` §6.

Changes: `AppHeader` simplified to `title="Routine"` (singular) + `leftAction` (calendar⇄list `IconButton`, replacing `PlannerBlock`'s former internal toggle) + `rightAction` (edit-mode toggle only — Regenerate and header "+" removed, since the footer's `OptimizeStrip` and "Add product" `Button` already call the same handlers and were left pixel-identical per the human's explicit instruction not to restyle them toward the mock's outlined-button look). `PlannerBlock` now renders only the Mo…Su day strip (dropped `viewMode`/`onViewModeChange`/`showWeekStrip`); the calendar-view's second `PlannerBlock` call (previously `showWeekStrip={false}`, rendering just the toggle) was deleted outright rather than kept as a prop-suppressed no-op, since nothing was left for it to render once the toggle moved to the header. `PeriodCard` (the accordion's card chrome — header row with icon/title/count/chevron, expand/collapse) is deleted; the selected period's groups render directly in the scroll content via one `NestableDraggableFlatList` instead of two always-mounted per-period ones, matching the mock (no card wrapping the group list). A plain (non-card) empty-state block is kept for a period with zero content while the other period has some — not shown in the mock's single Evening/Tu-21 screenshot, preserved as a judgment call rather than dropped silently.

Verification, same order as the original Phase 0-6 pass:
- `npx tsc --noEmit` — clean.
- `npx jest tests/routine-step-grouping tests/routine-engine/routines-screen-generation-ux.test.tsx` — 8/9 suites green, 75/76 tests green. The 1 red (`RoutinesScreen.completion.test.tsx`'s 01:00-boundary test) is the same pre-existing `getSkincareDateString` UTC bug already documented above and in Open items — untouched by this follow-up, still exercising the same unmodified helper.
- Read all 3 named qa-lead files (`RoutinesScreen.{grouping,completion,editMode}.test.tsx`) fully before touching the screen: none reference `expanded.morning/evening`, `PeriodCard`'s a11y state (`', expanded'`/`', collapsed'`) or header text, or press a period header to toggle it — confirmed via full-text read, not grep alone — so **zero edits were needed** to any of the three. One file **outside** that named set, `tests/routine-engine/routines-screen-generation-ux.test.tsx` (a different, pre-existing task's suite), asserted `getByLabelText('Regenerate routine')` twice and broke as expected once that header icon was removed; updated its 2 assertions to check the same functionality via the footer's `OptimizeStrip`/"Add product" affordances instead, mirroring the precedent already set once in this task's own Phase 0-6 history for this identical file.
- `npx jest --testPathIgnorePatterns="worktrees"` — full regression: 10 failing suites (194 passed, 2280/2359 tests passed), zero new regressions. Triaged each: 1 is the known `completion.test.tsx` UTC-boundary red above; the other 9 (`catalog-screen-hide-toggle.integration`, `RoutineCalendarView`, `ProductShelfCard`, `DetectedActiveBadgeWiring`, `product-shelf-card-hidden`, `PaoChip.integration`, `product-detail`, `catalog-screen`, `add-product-hub`) all fail on the same pre-existing stale-mock pattern the original tech-lead review already identified and reproduced against clean `dev` (`palette.plumTint`/`palette.goldenTint`/`shadow.sm` missing from partial `jest.mock('@/constants/tokens', ...)` fixtures, and one `getProductTypeBadgeStatus`-not-a-function case) — confirmed via grep that none of these 9 files import `RoutinesScreen.tsx`, `PlannerBlock.tsx`, `AppHeader.tsx`, or `PillToggle.tsx`, the only files this follow-up touched.

Not committed — left for tech-lead review per the coordinator's instruction.

**2026-08-28** — Tech-lead re-review (polish round 3, run directly by the coordinator for the same structural reason as the prior two rounds): `npx tsc --noEmit` clean; `npx jest tests/routine-step-grouping tests/routine-similar-product-priority` — 13/14 suites green, same single pre-existing UTC red, and the `routine-similar-product-priority` suite (covering `findSameSlotStep`'s separate add-time consumer) passing unchanged confirms the actives-only filter stayed scoped to the display layer as instructed, per grep-verified wiring (`groupHasActiveIngredientOverlap` used by both `DuplicateSlotWarningInline` and `RoutinesScreen`'s `similarMap`, `findSlotDuplicateGroups`/`findSameSlotStep` themselves untouched). Read `RoutineProductCard.tsx` in full: all requested changes present and correctly wired (`similarRow` — cobalt/`layers`, mirrors `conflictRow`'s structure; type badge removed, lightning + `AllergenBadge` kept; edge-to-edge photo via `ProductThumbnail`'s `fill`/`squareBottomLeft`; `radius.sm` corners; outer `cardShadow`/`cardShadowCompleted` wrapper). The self-caught `StyleSheet`-array-merge shadow bug (mutually-exclusive style objects instead of array-merging `shadow.none` over `shadow.sm`) is a real, non-obvious defect that predated this round and was invisible only because the old `overflow:hidden` bug was masking it — good catch, verified the fix is correct and covered by the new regression test. **Verdict: ACCEPT.**

**2026-08-28** — Coordinator, small direct follow-up (human-requested, one-line change, implemented directly rather than through the full engineer round-trip given the size): `StepTransitionDivider` between step groups (`RoutinesScreen.tsx`'s `renderGroupItem`) is now gated on `gamificationEnabled` — hidden entirely when gamification is off, since the human found the layering-note captions not worth showing without the completion-tracking feature they accompany. `renderGroupItem`'s `useCallback` deps updated to include `gamificationEnabled`. Verified: `npx tsc --noEmit` clean; `npx jest tests/routine-step-grouping/RoutinesScreen.grouping.test.tsx` 13/13 green (its fixture mocks `gamificationEnabled: true`, so the existing transition-matrix coverage is unaffected). No test added for the off-state specifically — flagged as a coverage gap, not blocking.

Also investigated, same session: human reported routine-card taps not always registering. Root cause identified directly in code (not a bug): `RoutineProductCard`'s tap only toggles completion when `tappable = gamificationEnabled && !isFutureSelection` — with gamification off, cards are never completion-tappable by design (edit mode opens the action sheet instead, unrelated to gamification). Noted a secondary, unconfirmed possibility for if the issue persists with gamification on: nested `Pressable`s inside `NestableDraggableFlatList` (`activationDistance={12}`) are a known source of intermittent missed taps in `react-native-draggable-flatlist` + `react-native-gesture-handler` (^4.0.3 / ~2.28.0) — not investigated further since the gamification-off explanation was confirmed sufficient this round.

## Bug-fix round (2026-08-28, engineer, branch `feature/routine-step-grouping`)

Real defects, not polish — three confirmed bugs fixed in one round per the coordinator's staged instructions (each one folded in "between steps" of the prior one, all reported together for approval and implemented once approved).

**Bug 1 — `RoutineSchedulerSheet` cross-writes Morning's schedule onto Evening (and vice versa).** Confirmed by the coordinator via direct code investigation + a user report of a stale "Retinobaza" density warning on days it isn't actually scheduled. Root cause: the sheet held ONE shared `scheduledDays` state for both periods, pre-populated via `deriveProductSchedule`'s "prefer morning, fall back to evening" merge (`(morningStep ?? eveningStep)?.scheduledDays ?? []`), then wrote that same array to BOTH periods on Save — silently overwriting one period's real per-`RoutineStep` schedule with the other's, even when the user only touched one period's toggle. Contradicts the task's own recorded decision: "`scheduledDays` stays on `RoutineStep` because it varies per period."

Fix:
- `src/utils/routineLabel.ts` — new `derivePeriodSchedules(routines, productId): { morning: {included, scheduledDays}, evening: {included, scheduledDays} }`, returning each period's OWN days, never merged. `deriveProductSchedule` (the old merged-value function) is left untouched — grepped every call site first (`ProductDetailScreen.tsx`'s read-only routine-label summary, `AddToRoutineSheet.tsx`'s own single-schedule add flow) and both still need the merged single-value contract for their own, different UI, so a new function was added instead of changing the existing one's signature.
- `src/components/routine/RoutineSchedulerSheet.tsx` — state split into `morningScheduledDays`/`eveningScheduledDays` (was one `scheduledDays`), both seeded independently from `derivePeriodSchedules` on open. UI: the single "Weekly Planner" section is replaced by two independent, conditionally-rendered sections — "Morning days" (only while Morning is toggled on) and "Evening days" (only while Evening is toggled on) — each with its own `WeeklySchedulePicker`, so the UI can never imply a unified schedule the data model doesn't have. Toggling a period on for the first time starts its picker at `[]` ("every day") because that's what `derivePeriodSchedules` already returns for an absent step — no copy-from-the-other-period path exists. `handleSave` now writes `morningScheduledDays` to the morning step only and `eveningScheduledDays` to the evening step only.
- Noted, not fixed (out of this bug's scope): `AddToRoutineSheet.tsx`'s `StepSchedule` step has the identical one-shared-`scheduledDays`-for-both-periods shape when adding a product that's already scheduled in one period and the user also checks the other period in the same session — same class of bug, different screen. Flagged for a follow-up task; not touched here since the coordinator scoped this round to `RoutineSchedulerSheet` specifically.

**Bug 2 — `RoutinesScreen` doesn't reliably/instantly reflect a completion toggle.** Confirmed by the coordinator via direct code investigation. `isCompleted`/`toggleCompleted` are stable function references from `completionStore.ts`'s creator — `set()` only ever replaces `completions`/`hydrated`, never these methods — so selecting only those two (`useCompletionStore((s) => s.isCompleted)` / `(s) => s.toggleCompleted)`) never changes by `Object.is`, and Zustand's `useSyncExternalStore`-based subscription never re-renders `RoutinesScreen` after a toggle; the card only appeared to update when some unrelated re-render fired afterward (day switch, edit-mode toggle, etc.). Also explains a related human report ("tapping a completed card a second time doesn't revert it") — verified `completionStore.toggleCompleted` already does a true toggle and `RoutineProductCard.handlePress` calls `onToggleComplete` unconditionally on every tap regardless of current `completed` state, so the underlying logic was always correct; only the missing re-render made the second tap look like a no-op.

Fix: `src/screens/RoutinesScreen.tsx` — added `const completions = useCompletionStore((s) => s.completions);` alongside the existing `isCompleted`/`toggleCompleted` selectors, and added `completions` to the two `useCallback` dependency arrays (`renderGroupMember`, `renderGroupItem`) whose closures read `isCompleted`. Grepped for every other `useCompletionStore` consumer in `src/` — `RoutinesScreen.tsx` is the only call site, so no other component needed the same fix.

**Bug 3 verification (not a new bug) — double-tap round-trip.** The coordinator asked to confirm, not assume, that a second tap on a completed card correctly reverts it once Bug 2 is fixed. Confirmed: `completionStore.toggleCompleted` removes-if-exists/adds-if-not (a true toggle) and `RoutineProductCard.handlePress` calls it unconditionally — the round-trip was always logically correct; only the re-render was missing. Verified with a new test that taps twice in sequence and asserts the completed-badge appears/disappears in the same render pass each time (see Tests below) — it passes with the Bug 2 fix, confirming no separate defect.

**Card layout change (human-requested directly, same round) — relocate zap icon / edit-mode icons / Completed badge onto the brand-name row.** Two staged asks, the second correcting the first: (1) move the always-on active-ingredient lightning icon and, in edit mode, the Pause/Schedule icons off `rightCluster` onto the same line as `brandName`; (2) correction — the "Completed" badge moves there too, not staying in `rightCluster` as first guessed. Only `AllergenBadge` stays behind in `rightCluster`, visibility rule unchanged (hidden in edit mode or once completed, exactly as it was as part of the old combined right-cluster switch).

`src/components/routine/RoutineProductCard.tsx`:
- New `headerRow` (flex row, `justify-content: space-between`) inside `textColumn`, rendered above `productName`. Holds `brandName` (flex:1, when present) plus one state-dependent slot (`headerRowIcons`) with the same priority the old `rightCluster` switch used: `editMode` (Pause/Schedule `IconButton`s) > `completed` (`Badge testID="completed-badge"`) > active ingredient (`View testID="active-badge-${key}"` + zap `Icon`) > nothing. Only relocated — none of the three occupants' own visibility conditions changed (same testIDs, same `stopPropagation` press handlers).
- When `product.brand` is absent, `headerRow` still renders for the icon slot alone (`showHeaderRow = !!product.brand || !!headerRowIcons`) rather than moving the slot onto `productName`'s line — keeps `productName`'s line reserved for the name only and keeps the icon's vertical position consistent across products regardless of whether they have a brand.
- `rightCluster` simplified to `!editMode && !completed ? <AllergenBadge .../> : null`; the wrapping `<View style={styles.rightCluster}>` is now only rendered when there's something to show (was previously always rendered, even empty, during edit mode).
- Removed the unused `statusBadgesRow` style (its only occupants, zap + AllergenBadge, no longer share a wrapper).

**Inner-shadow removal (human-requested directly, same round).** `RoutineProductCard.tsx` — deleted `InnerShadowOverlay` (the SVG gradient overlay rendered on completed cards) and its call site, plus the now-unused `Svg`/`LinearGradient`/`Rect`/`Stop`/`Defs` import (`react-native-svg` itself stays a dependency, just no longer imported by this file). Rest of the completed state (badge, `cardCompleted` border, dimmed text/thumbnail) is untouched. No test referenced `testID="completed-inner-shadow"` (grepped `tests/` and `src/` first), so no test file needed updating for this specific removal.

**Tests:**
- `src/utils/routineLabel.test.ts` — 4 new cases for `derivePeriodSchedules` (not-in-any-routine, independent per-period days, absent period returns `{included:false, scheduledDays:[]}` rather than the other period's days, no-match-for-a-different-product).
- `tests/vials-bottomsheet-consolidation/RoutineSchedulerSheet.regression.test.tsx` — the old single "renders ... Weekly Planner" assertion is replaced (intentional behavior change, this bug fix's whole point) with a "no picker renders until its period is toggled on" case, plus 3 new regression cases directly proving Bug 1's fix: independent pre-population from each period's own step, no-cross-write on Save when only one period's picker is touched, and a newly-toggled-on Evening picker starting at "every day" rather than copying Morning's days.
- `tests/routine-step-grouping/RoutinesScreen.completion.test.tsx` — the `@/store/completionStore` mock is now backed by a REAL Zustand `create()` store (previously a plain `jest.fn((selector) => selector({...}))`, which re-evaluates fresh on every render regardless of whether the component actually subscribed to anything and so could never have caught Bug 2). `isCompleted`/`toggleCompleted` still delegate to the same `mockIsCompleted`/`mockToggleCompleted` spies the rest of the file's `toHaveBeenCalledWith` assertions rely on; `toggleCompleted` additionally bumps a `completions` array's identity via `set()`, mirroring production. New test taps a card twice and asserts the completed-badge appears/disappears in the same render pass each time.
- `tests/routine-step-grouping/RoutineProductCard.test.tsx` — no changes needed; every existing assertion queries by `testID`/text, none by structural position, so all 29 cases (including the edit-mode and completed-state ones) passed unchanged against the relocated icons.

Verification:
- `npx tsc --noEmit` — clean throughout.
- `npx jest tests/routine-step-grouping tests/vials-bottomsheet-consolidation tests/routine-similar-product-priority tests/routine-engine/routines-screen-generation-ux.test.tsx src/utils/routineLabel.test.ts src/store/completionStore.test.ts` — 197/198 tests green; the 1 red is the same pre-existing `getSkincareDateString` UTC-boundary bug documented throughout this file (`RoutinesScreen.completion.test.tsx`'s 01:00-boundary test), untouched by this round.
- `npx jest --testPathIgnorePatterns="worktrees"` — 10 failing suites (194 passed, 2302/2381 tests passed) — identical suite names to every prior baseline in this file (`catalog-screen-hide-toggle.integration`, `RoutinesScreen.completion` [known UTC bug], `RoutineCalendarView`, `ProductShelfCard`, `DetectedActiveBadgeWiring`, `product-shelf-card-hidden`, `PaoChip.integration`, `product-detail`, `catalog-screen`, `add-product-hub`) — zero new regressions.

Not committed — left for tech-lead review per the coordinator's instruction.

**2026-08-28** — Tech-lead re-review (bug-fix round, run directly by the coordinator for the same structural reason as every prior round today): independently re-derived and confirmed the completion-store reactivity root cause before the engineer's report arrived (Zustand selector on a stable method reference never triggers a re-render — `RoutinesScreen.tsx:134-135` selecting `s.isCompleted`/`s.toggleCompleted` alone). Read `RoutineSchedulerSheet.tsx` in full: `morningScheduledDays`/`eveningScheduledDays` are genuinely independent state, pre-populated via the new `derivePeriodSchedules` and saved back to only their own period's step — no cross-write path remains. Read `RoutinesScreen.tsx`'s `completions` subscription and its presence in the `renderGroupItem` deps array. Read `RoutineProductCard.tsx` in full: `headerRowIcons` priority (edit icons > completed badge > active icon) matches the corrected instruction (Completed badge included, not left behind as I'd first guessed), `AllergenBadge` alone remains in `rightCluster`, `InnerShadowOverlay` and its `react-native-svg` import are gone. Re-ran `npx tsc --noEmit` (clean) and the full affected-suite set myself (`tests/routine-step-grouping`, `tests/routine-similar-product-priority`, `tests/vials-bottomsheet-consolidation/RoutineSchedulerSheet.regression.test.tsx`) — 132/133 green, same single pre-existing UTC red. Confirmed the new double-tap round-trip test (`RoutinesScreen.completion.test.tsx`, describe block "bug fix: completion re-render + double-tap round-trip (2026-08-28)") exists and passes. **Verdict: ACCEPT.**

## Bug-fix round (2026-08-31, engineer, branch `feature/routine-step-grouping`) — `ConflictWarningInline` period-scoping bug

**Bug — `RoutinesScreen`'s `listHeader` fed `ConflictWarningInline` BOTH periods' steps unconditionally, regardless of `selectedPeriod`.** Confirmed via live device testing with the human, root cause pre-diagnosed by the coordinator (not re-diagnosed here): `ConflictWarningInline` was correct when the screen showed both Morning and Evening accordions simultaneously (pre-Phase-7). Since Phase 7 replaced that with the single-active-period `PillToggle` (only `selectedPeriod`'s cards are ever rendered — `activeRenderGroups = selectedPeriod === 'morning' ? amRenderGroups : pmRenderGroups`), the call site went stale: the banner kept evaluating conflicts/advisories/density across BOTH periods, so it could reference a product with zero presence in the period actually on screen. Confirmed live: a retinoid product with ONLY an evening step still showed its "Sensitivity note" advisory while the Morning tab was selected.

Fix, scoped to the call site (`src/screens/RoutinesScreen.tsx`, `listHeader`'s `ConflictWarningInline` call, ~line 690-697) — `ConflictWarningInline` itself (`src/components/routine/ConflictWarningInline.tsx`) is untouched, per the coordinator's framing that it is a well-tested, reusable component and neither `tests/skin-conditions/condition-advisory-rows.test.tsx` nor `tests/conflict-matrix-expansion/ConflictWarningInline.severity-prefix.test.tsx` assert that merging both periods is required behavior:

```tsx
<ConflictWarningInline
  morningSteps={selectedPeriod === 'morning' ? amSteps : []}
  eveningSteps={selectedPeriod === 'evening' ? pmSteps : []}
  products={products}
  skinConditions={profile?.skinConditions ?? []}
/>
```

`selectedPeriod` was already present in `listHeader`'s `useMemo` dependency array (it seeds the `PillToggle` rendered in the same block), so no deps-array change was needed.

**Test-scope check (per the coordinator's instruction, not re-diagnosis):** read `tests/routine-step-grouping/RoutinesScreen.{grouping,completion,editMode}.test.tsx` in full — none reference `ConflictWarningInline` at all (that component isn't mocked in any of the three, but none of their assertions touch it or its rendered rows), so **zero edits were needed** to any of the three. Grepped every other `tests/` file that either renders `RoutinesScreen` or mocks `ConflictWarningInline`; the only files rendering it directly are `tests/skin-conditions/condition-advisory-rows.test.tsx` (renders the component in isolation with hand-built props, unaffected by a `RoutinesScreen` call-site change) and the two files noted above that assert only copy/severity-prefix behavior on the component itself.

New test file: `tests/routine-step-grouping/RoutinesScreen.conflictBannerPeriodScope.test.tsx` — two cases, both driving the REAL `ConflictWarningInline` (not mocked) through a fully-rendered `RoutinesScreen`, using the same mock scaffold as `RoutinesScreen.grouping.test.tsx` plus a `profileStore` mock carrying `skinConditions: ['eczema']` (the exact mechanism — `src/utils/skinConditionModifiers.ts`'s eczema condition modifier flags `retinoid` — behind the live "Sensitivity note" bug report): (1) an evening-only retinoid's card and its "Sensitivity note" advisory are both absent while Morning is selected, and both appear after pressing the real `PillToggle`'s Evening tab; (2) the symmetric case (morning-only retinoid, advisory visible on Morning, gone after switching to Evening). Verified both cases fail for the right reason against the pre-fix code (temporarily reverted the fix, reran, confirmed both assertions failed on the stale-advisory-still-visible line, then restored the fix) before finalizing.

Verification:
- `npx tsc --noEmit` — clean.
- `npx jest tests/routine-step-grouping` — 8/9 suites, 85/86 tests green (the new file's 2/2 pass); the 1 red is the same pre-existing `RoutinesScreen.completion.test.tsx` UTC-boundary bug documented throughout this file, untouched by this round.
- `npx jest tests/routine-similar-product-priority tests/routine-engine/routines-screen-generation-ux.test.tsx tests/routine-engine/goal-confirm-routines-screen.test.tsx` — 8/8 suites, 44/44 tests green (these are the other suites that render a fully-composed `RoutinesScreen` or exercise `ConflictWarningInline`-adjacent wiring).
- `npx jest --testPathIgnorePatterns="worktrees"` — 10 failing suites (195 passed, 2304/2383 tests passed), suite names identical to every prior baseline in this file (`catalog-screen-hide-toggle.integration`, `RoutinesScreen.completion` [known UTC bug], `RoutineCalendarView`, `ProductShelfCard`, `DetectedActiveBadgeWiring`, `product-shelf-card-hidden`, `PaoChip.integration`, `product-detail`, `catalog-screen`, `add-product-hub`) — zero new regressions.

Not committed — left for tech-lead review per the coordinator's instruction.

## Code-review round (2026-08-31, engineer, branch `feature/routine-step-grouping`)

`/code-review` ran against the full diff and surfaced 10 findings. Fixed 1-7 in priority order; 8-9 deliberately skipped with rationale below (per the review's own "use judgment" allowance for the lowest tier).

**1 — `ConflictWarningInline` cross-period conflict detection was permanently disabled (Priority 1, active regression).** The 2026-08-31 fix earlier the same day (see the "ConflictWarningInline period-scoping bug" round above) scoped BOTH `morningSteps` and `eveningSteps` to only `selectedPeriod`, so `allSteps = [...morningSteps, ...eveningSteps]` inside the component could never contain both periods at once — a real AM+PM pairwise ingredient conflict (e.g. a retinoid used in the morning, an AHA acid used in the evening) could never fire again, regardless of which tab was open. That fix correctly solved the advisory/density staleness bug it targeted but went too far by sharing one `allSteps` local for BOTH conflicts and advisories.

Fix: gave `ConflictWarningInline` a new optional prop, `allSteps?: RoutineStep[]`, used ONLY for `ConflictEngine.detectConflicts` — defaults to `[...morningSteps, ...eveningSteps]` so every existing caller (both `tests/skin-conditions/condition-advisory-rows.test.tsx` and `tests/conflict-matrix-expansion/ConflictWarningInline.severity-prefix.test.tsx`, which call it directly with hand-built props and no new prop) is unaffected. Condition advisories (`getConditionRiskWarnings`) and density findings (`getActiveDensityFindings`) keep reading the tab-scoped `morningSteps`/`eveningSteps` exactly as before — those are the two rows that actually caused the "notification for a product not shown in this tab" confusion the human originally reported, not pairwise conflicts. `RoutinesScreen.tsx`'s `listHeader` now passes `allSteps={[...amSteps, ...pmSteps]}` alongside the already tab-scoped `morningSteps`/`eveningSteps`.

New test file `tests/routine-step-grouping/RoutinesScreen.conflictCrossPeriod.test.tsx` (3 cases): a retinoid (AM-only) + AHA peeling product (PM-only) fires "Ingredient conflict" while Morning is selected even though the AHA card itself is correctly absent from the rendered list; the conflict keeps firing after switching to Evening; a sanity control where only one of the pair is scheduled at all shows no conflict. Re-ran `tests/routine-step-grouping/RoutinesScreen.conflictBannerPeriodScope.test.tsx` (the advisory-scoping regression test from the earlier round) unchanged — still green, proving both properties hold simultaneously.

**2 — `gamificationEnabled` default flip to `true` (Priority 1, business-level contradiction).** Investigated per the review's request rather than silently picking a side. `PRD_Spec.md` §6.2 explicitly states the default changes to `true` and that this "supersedes engine 4.0/SCREENS.md §5, which specifies Default OFF" — so the flip was a deliberate, documented decision by this task's own spec, not an oversight. But CLAUDE.md's Key Constraints are unambiguous ("Gamification is opt-in — default OFF") and are marked as instructions that "OVERRIDE any default behavior" — a feature-level spec document asserting it "supersedes" a top-level, human-authored product constraint is exactly the tech-design-template.md "Type D — Contradiction" gap type, which should have been routed back for human sign-off rather than written in as a settled assumption.

Resolution: reverted `DEFAULT_SETTINGS.gamificationEnabled` back to `false` in `src/store/settingsStore.ts`. Completion tracking stays fully built and shipped — it is simply off until the user opts in via the existing Profile "Gamification" toggle, whose copy ("Track what you've used by tapping products in your routine") already describes the real feature rather than the old rewards/streaks framing. This also fully resolves the secondary migration gap the review flagged (existing installs that already persisted `gamificationEnabled: false` would never have seen the new default anyway) — with the default reverted, there is no divergence between new and existing installs at all.

**3 — `AddToRoutineSheet.tsx` had the identical shared-`scheduledDays`-across-AM/PM bug** `RoutineSchedulerSheet.tsx` was fixed for earlier (bug-fix round 2026-08-28, same file `derivePeriodSchedules` in `src/utils/routineLabel.ts` reused, not duplicated). Split the sheet's single `scheduledDays` state into `morningScheduledDays`/`eveningScheduledDays`; `handleProductSelect` now pre-populates both independently via `derivePeriodSchedules`; the save queue (`runQueue`/`handleSave`) now tags each queued period with `period: 'morning' | 'evening'` so it — and the duplicate-slot resolution handlers `handleReplaceDuplicate`/`handleKeepBothDuplicate`, which needed the same tag added to `duplicateConflict`'s state shape — always write back only that period's own days, never the other's. `StepSchedule`'s single "Weekly Planner" section replaced by two independent, conditionally-rendered "Morning days"/"Evening days" `WeeklySchedulePicker` sections, mirroring `RoutineSchedulerSheet`'s established UI pattern exactly.

Tests: `tests/routine-similar-product-priority/add-to-routine-sheet-duplicate-flow.test.tsx` gained 2 new cases (pre-population from + save-back to independent per-period days when only one picker is edited; a newly-checked Evening picker starts at "every day", never copying Morning's days) — same shape as `RoutineSchedulerSheet.regression.test.tsx`'s existing per-period bug-fix cases. All 7 pre-existing cases in that file pass unchanged (all fixtures there use `scheduledDays: []` on both periods, so the split is invisible to them).

**5 — `ProductDetailScreen.tsx` displayed the OLD merged schedule.** `formatUsedSummary` (the "Used: …" hero bar) now takes `PeriodSchedules` (via the new `derivePeriodSchedules`, not `deriveProductSchedule`) and shows each period's own days distinctly when they diverge (`"Morning: daily · Evening: Mon • Wed • Fri"`), while collapsing back to the original single-line format (`"Morning & Evening, daily"`) when both periods agree — so no regression in the common case. The sun/moon hero icon now reads `periodSchedules.morning/evening.included` instead of the old merged booleans. Left the footer CTA button (`routineLabel`/`formatRoutineLabel`, still backed by `deriveProductSchedule`) unchanged — it's a compact button label rather than the schedule "display" the review named, and `RoutineSchedulerSheet` (opened by that same button) already shows the accurate per-period detail. `deriveProductSchedule` itself is untouched (still needed by that button and by `AddToRoutineSheet`'s own pre-existing-schedule check).

Incidental fix: `tests/inci-attribution-highlighting/DetectedActiveBadgeWiring.test.tsx` fully replaces `@/utils/routineLabel` with `jest.mock(() => ({...}))` (no `derivePeriodSchedules` key) and renders `ProductDetailScreen` — added the missing export (returning both periods `{ included: false, scheduledDays: [] }`) so the mock doesn't throw a new `TypeError` on top of its pre-existing, already-documented `getProductTypeBadgeStatus` stale-mock failure (this suite is in every prior baseline's known-red list; confirmed the failure signature is still exactly that one pre-existing bug, not a new one).

**6 — `duplicateSlot.ts`'s `fingerprint()` didn't sort `zones` before joining**, so `['face','neck']` vs `['neck','face']` (easily produced since `ProductDetailScreen`'s zone toggle always appends) fingerprinted differently and were wrongly treated as non-duplicates. Fixed with `[...zones].sort().join(',')`. New test in `src/utils/routineEngine/duplicateSlot.test.ts`: two products tagged with the same zone set in reversed order still group as duplicates.

**7 — `findSlotDuplicateGroups(steps, products: Product[] = [])`'s silent default.** Checked both live call sites (`RoutinesScreen.tsx`, `DuplicateSlotWarningInline.tsx`) — both already pass `products` explicitly, so nothing reasonable breaks. Made `products` required (dropped the `= []` default); the 7 unit-test call sites in `duplicateSlot.test.ts` that intentionally omit it now pass `[]` explicitly — same asserted behaviour (the documented pre-Phase-6 fallback fingerprint), just no longer reachable by omission. A future third caller can no longer accidentally revert to that fallback by forgetting the argument.

**8 — `getPrimaryActiveKey()` duplication (RoutineProductCard / activeBadges.ts / RoutineStepCard) — not consolidated.** Confirmed the premise: `RoutineProductCard.tsx` exports its own `getPrimaryActiveKey`, `activeBadges.ts` has related-but-differently-shaped `getActiveBadgeCategory`/`getProductActiveBadgeKeys`, and `RoutineStepCard.tsx` has its own inline equivalent under a different name. Left unconsolidated: `RoutineStepCard.tsx` is the component this task's own Phase 0-6 log already deliberately decided NOT to touch (kept in place only for 2 unrelated standalone test suites that still import it directly), and reopening it this late in a bug-fix round — for three call sites whose dedup/sort needs may not be identical — is exactly the "rabbit hole" the review itself flagged. Judgment: skip.

**9 — `completionStore.ts` unbounded linear scan — skipped**, per the review's own instruction (performance observation, not a bug; explicitly gated on 1-6 being done "with plenty of time left" — item 8 already used the discretionary time budget on investigation rather than a code change).

Files modified this round: `src/components/routine/ConflictWarningInline.tsx`, `src/screens/RoutinesScreen.tsx`, `src/store/settingsStore.ts`, `src/screens/ProfileScreen.tsx`, `src/components/routine/AddToRoutineSheet.tsx`, `src/screens/ProductDetailScreen.tsx`, `src/utils/routineEngine/duplicateSlot.ts`, `src/utils/routineEngine/duplicateSlot.test.ts`, `tests/inci-attribution-highlighting/DetectedActiveBadgeWiring.test.tsx`, `tests/routine-similar-product-priority/add-to-routine-sheet-duplicate-flow.test.tsx`.

New: `tests/routine-step-grouping/RoutinesScreen.conflictCrossPeriod.test.tsx`.

Verification:
- `npx tsc --noEmit` — clean.
- `npx jest tests/routine-step-grouping tests/routine-similar-product-priority tests/vials-bottomsheet-consolidation src/utils/routineEngine/duplicateSlot.test.ts tests/inci-attribution-highlighting/DetectedActiveBadgeWiring.test.tsx tests/contribution-consent/ProfileScreenContributionToggle.test.tsx` — 23/25 suites, 198/212 tests green; the 2 red are both pre-existing and unrelated (`RoutinesScreen.completion.test.tsx`'s documented UTC-boundary bug; `DetectedActiveBadgeWiring.test.tsx`'s documented `getProductTypeBadgeStatus`-stale-mock bug in `RoutineStepCard.tsx`, confirmed still the same single failure signature after this round's mock fix, not a new one).
- `npx jest --testPathIgnorePatterns="worktrees"` — 10 failing suites (196 passed, 2310/2389 tests passed), suite names identical to every prior baseline in this file (`catalog-screen-hide-toggle.integration`, `RoutinesScreen.completion` [known UTC bug], `RoutineCalendarView`, `ProductShelfCard`, `DetectedActiveBadgeWiring`, `product-shelf-card-hidden`, `PaoChip.integration`, `product-detail`, `catalog-screen`, `add-product-hub`) — zero new regressions.

Not committed — left for tech-lead review per the coordinator's instruction.
