Bone-surface container with a hairline border — the core layout primitive.

```jsx
<Card padding="md">Routine summary</Card>
<Card variant="raised" interactive>Tap to open product</Card>
```

Variants: `surface` (bone, default), `raised` (white + shadow), `flat` (border only). Set `interactive` for tappable cards.
