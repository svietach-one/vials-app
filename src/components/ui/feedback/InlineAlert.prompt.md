Contextual notice — ingredient conflicts, PAO expiry, fading-zone transitions, info banners.

```jsx
<InlineAlert tone="warning" icon={<i data-lucide="triangle-alert" />} title="Conflict">
  Don't layer retinol with this AHA tonight.
</InlineAlert>
```

Tones: `sos` · `warning` · `safe` · `info`. Tinted small surface — never a full accent fill.
