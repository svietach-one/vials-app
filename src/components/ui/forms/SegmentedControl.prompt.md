Monochrome segmented switch for mutually-exclusive modes.

```jsx
<SegmentedControl options={["Morning", "Evening"]} value={phase} onChange={setPhase} />
```

Active segment is a white pill on a sunken track. Pass `{value,label}` objects or plain strings.
