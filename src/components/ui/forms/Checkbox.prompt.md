Routine-step checkbox. The checked state fills Cabernet — Vials' "active trigger" signal.

```jsx
<Checkbox checked={done} onChange={setDone} label="Apply retinoid" sublabel="Pea-size, avoid eyes" />
```

Checking strikes through the label. Sizes `sm` / `md`.
